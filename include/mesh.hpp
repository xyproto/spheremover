#pragma once

// WORK IN PROGRESS

// A mesh is a collection of faces?
// A face is a collection of vertex indices + the lookup table? Hash map?

// Go: map[VertexIndex]Vec3

// An object is a position + rot + scale + a mesh?
